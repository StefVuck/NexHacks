# Copy this to terraform.tfvars and fill in values

swarm_id         = "my-swarm-demo"
aws_region       = "us-east-1"
instance_type    = "t3.micro"

# Restrict SSH to your IP for security
ssh_allowed_cidr = "YOUR_IP/32"

# Auto-terminate after 2 hours (0 to disable)
auto_destroy_hours = 2
